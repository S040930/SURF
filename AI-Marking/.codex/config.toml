# The r21 MCP server (start.mcp.sh) was removed when the experiment stack
# migrated to the memory-study worker architecture.  Keeping it registered
# here made every Codex session fail at bootstrap with
# "No such file or directory (os error 2)".